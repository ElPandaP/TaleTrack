namespace TaleTrackApp.Model;

/// <summary>Persisted by name (see <c>AppDbContext</c>), so the names are part of the schema and the API contract.</summary>
public enum MediaType
{
    Movie,
    Series,
    Book
}
